{
  "name": "comment-section",
  "version": "0.0.1",
  "description": "Semi-automatic management comment blocks for dirty code :)",
  "main": "dist/index.js",
  "types": "dist/index.ts",
  "scripts": {
    "build": "tsup src/index.ts --format esm,cjs && tsc src/index.ts --declaration --emitDeclarationOnly --outDir dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.10.4",
    "tsup": "^8.0.1",
    "typescript": "^5.3.3"
  }
}
